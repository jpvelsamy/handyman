package in.handyman.raven.lib;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.node.ObjectNode;
import in.handyman.raven.exception.HandymanException;
import in.handyman.raven.lambda.action.ActionExecution;
import in.handyman.raven.lambda.action.IActionExecution;
import in.handyman.raven.lambda.doa.audit.ActionExecutionAudit;
import in.handyman.raven.lib.model.IncidentManagement;
import java.lang.Exception;
import java.lang.Object;
import java.lang.Override;

import okhttp3.*;
import org.slf4j.Logger;
import org.slf4j.Marker;
import org.slf4j.MarkerFactory;


/**
 * Auto Generated By Raven
 */
@ActionExecution(
    actionName = "IncidentManagement"
)
public class IncidentManagementAction implements IActionExecution {
  private final ActionExecutionAudit action;

  private final Logger log;

  private final IncidentManagement incidentManagement;

  private final Marker aMarker;

  private final ObjectMapper mapper = new ObjectMapper();
  private static final MediaType MediaTypeJSON = MediaType
          .parse("application/json; charset=utf-8");

  private final String URI  ;

  public IncidentManagementAction(final ActionExecutionAudit action, final Logger log,
      final Object incidentManagement) {
    this.incidentManagement = (IncidentManagement) incidentManagement;
    this.action = action;
    this.log = log;
    this.aMarker = MarkerFactory.getMarker(" IncidentManagement:"+this.incidentManagement.getName());
    this.URI=action.getContext().get("copro.feedback.url");

  }

  @Override
  public void execute() throws Exception {
    OkHttpClient httpclient = new OkHttpClient();

    final ObjectNode objectNode = mapper.createObjectNode();

    objectNode.put("templateId",incidentManagement.getTemplateId());
    objectNode.put("assetId",incidentManagement.getAssetId());
    objectNode.put("payload",incidentManagement.getPayload());

    // build a request
    Request request = new Request.Builder().url(URI)
            .post(RequestBody.create( objectNode.toString(),MediaTypeJSON)).build();
    log.info(aMarker, "The request got it successfully the templateId and assetId and payload {} {} {}",incidentManagement.getTemplateId(),incidentManagement.getAssetId(),incidentManagement.getPayload());

    try (Response response = httpclient.newCall(request).execute()) {
      String responseBody = response.body().string();
      String name = incidentManagement.getName() + "feedback response";
      if (response.isSuccessful()) {
        action.getContext().put(name, responseBody);
        log.info(aMarker, "The Successful Response  {} {}", name, responseBody);
      }else {
        log.info(aMarker, "The Failure Response  {} {}", name, responseBody);
      }
    }catch (Exception e){
      log.info(aMarker, "The Exception occurred ",e);
      throw new HandymanException("Failed to execute", e);
    }
  }

  @Override
  public boolean executeIf() throws Exception {
    return incidentManagement.getCondition();
  }
}
