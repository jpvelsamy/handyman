package in.handyman.raven.lib;

import in.handyman.raven.action.ActionExecution;
import in.handyman.raven.action.IActionExecution;
import in.handyman.raven.lib.model.GoogleContactFetchAll;
import in.handyman.raven.process.Context;
import lombok.extern.log4j.Log4j2;
import org.apache.logging.log4j.MarkerManager;

/**
 * Auto Generated By Raven
 */
@ActionExecution(
        actionName = "GoogleContactFetchAll"
)
@Log4j2
public class GoogleContactFetchAllAction implements IActionExecution {
    private final Context context;

    private final GoogleContactFetchAll googleContactFetchAll;

    private final MarkerManager.Log4jMarker aMarker;

    public GoogleContactFetchAllAction(final Context context, final Object googleContactFetchAll) {
        this.googleContactFetchAll = (GoogleContactFetchAll) googleContactFetchAll;
        this.context = context;
        this.aMarker = new MarkerManager.Log4jMarker("GoogleContactFetchAll");
        this.context.getDetailMap().putPOJO("context", context);
    }

    @Override
    public void execute() throws Exception {
    }

    @Override
    public boolean executeIf() throws Exception {
        return false;
    }
}
