package in.handyman.raven.lib;

import in.handyman.raven.action.ActionExecution;
import in.handyman.raven.action.IActionExecution;
import in.handyman.raven.lib.model.GoogleContactCreate;
import in.handyman.raven.process.Context;
import lombok.extern.log4j.Log4j2;
import org.apache.logging.log4j.MarkerManager;

/**
 * Auto Generated By Raven
 */
@ActionExecution(
        actionName = "GoogleContactCreate"
)
@Log4j2
public class GoogleContactCreateAction implements IActionExecution {
    private final Context context;

    private final GoogleContactCreate googleContactCreate;

    private final MarkerManager.Log4jMarker aMarker;

    public GoogleContactCreateAction(final Context context, final Object googleContactCreate) {
        this.googleContactCreate = (GoogleContactCreate) googleContactCreate;
        this.context = context;
        this.aMarker = new MarkerManager.Log4jMarker("GoogleContactCreate");
        this.context.getDetailMap().putPOJO("context", context);
    }

    @Override
    public void execute() throws Exception {
    }

    @Override
    public boolean executeIf() throws Exception {
        return false;
    }
}
